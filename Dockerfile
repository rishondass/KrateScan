FROM node:22-alpine

WORKDIR /app

COPY . .

RUN npm install

ARG MONGODB_URL

ENV MONGO_URL=$MONGODB_URL

ARG NEXTAUTH_SECRET

ENV NEXTAUTH_SECRET=$NEXTAUTH_SECRET

ARG AUTH_URL

ENV AUTH_URL=$AUTH_URL

RUN npm run build

EXPOSE 3000

CMD ["npm", "run", "start"]